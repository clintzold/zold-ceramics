<% if flash.any? %>
  <div class="container-fluid d-flex flex-column align-items-center p-0 m-0">
  <% flash.each do |type, msg| %>
    <div class="alert alert-<%= type.to_s %> m-0 w-100 text-center"><%= msg %></div>
  <% end %>
  </div>
<% end %>
